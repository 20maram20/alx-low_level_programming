#include "main.h"
#include <stdio.h>

/**
 * _isupper fumction -  check if c is upper case
 * @c:is input for function
 * Return: 1 if upper case and 0 if not.
 *
*/
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
else
return (0);
}
}
